04.29 15:21
3 12
Scala编程课：项目创建与基础语法练习
2026-03-12  15:16
【智能总结】
本次课堂围绕Scala编程展开，讲解项目创建、基础语法，并布置多项练习。
- **创建Scala项目**：先建立项目，项目内结构与Java类似，含包和连接库。在SRC资源库右键可建立Scala class。
- **Scala变量与常量**：定义变量和常量可省略分号，var定义变量，val定义常量。
- **Scala入口函数**：与Java不同，Scala用def main定义入口函数，Args为参数，类型是Array[String]。
- **Scala对象声明**：声明变量时类型由赋值决定，声明的变量可视为对象，类似Java但更灵活。
- **类的可见性**：Scala类默认修饰符为public，具有公有可见性，与大数据特性相关。
- **课堂练习**：
    - **练习1**：创建列表赋值，输出偶数项。
    - **练习2**：奇数项相加求和输出。
    - **练习3**：奇数项与偶数项对应相乘后求和输出。 
    - **注意事项**：运行项目需定义main方法作为入口，可写类和对象，或直接用object。
【原文】
说话人 1
啊，不要说话了啊，咱们。
说话人 2
你们这边笔记本呢？在这呢。
说话人 1
就每一排都有了是吧？哎，咱们再看一遍这个怎么创建这个创建这个项目啊。再看一遍，然后咱们这节课啊。做个做个练习哈。
说话人 2
哦。
说话人 1
怎么呢？建议看一下。
说话人 3
我上次计算机二级没过。二级老难了。你报的 Python 还是？让你去报的是 Python Python 你过了吗？没。
说话人 1
啊？
说话人 3
我没有，我报了三个，就去了一个。
说话人 1
我靠，时间不够。
说话人 3
对呀。然后这次又报了，报了 WPS。WPS 比较好。我上次 Python 考50多分。他他是不是过？浩浩成浩浩成他过了吗？这次我一定要过。太贵了，妈的，一个月100块钱。
说话人 1
这样呢我们就建立了第一个这个项目名称啊，First，啊建立了第一个项目，是 Gala 项目。建立项目之后啊，我们可以看到这里面跟我们这个以前学的 Java 是差不多的啊，这里面有扎包。然后有 Scala 的连接库，对吧？然后我们在 SRC 啊，这里是我们的资源库啊。在资源里面呢，我们点击右键啊。电脑比较慢啊，大家稍微等一下。点击右键之后啊，我们可以建立类啊，建立类。比方说这里有 Scala class，对吧？哎，这叫 Scala class。下面下面的这些啊，我们都先不要去关注它。比方说我们有 Scala class，是 create object，对吧？这有很多，比方说 APP 等等系列的。随着我们课的课程的深入啊，我会给大家逐渐地来讲这些东西啊。比方说我们现在先建立一个 class，看它与我们之前所学的这个 Java 类啊有什么不同啊。给大家简单地说一下啊。然后我现在先建一个 class 类啊，然后呢第一个类呢我们叫做测试法太。
说话人 2
卡了。
说话人 1
因为我这个机器可能开的比较多了。
说话人 2
可能开的比较多了。卡死了。
说话人 1
建立一个 test 类啊 test 类，然后呢我们就可以 finish。 
说话人 1
太卡了，这可能是啊，我先调整调整我笔记本。
说话人 1
不好意思啊，刚才打断一下。大家现在看一下啊。首先呢，我们现在是念一个 Scala 不在，比如说新建一个 Scala 项目。我们这个项目名称啊，叫这个。 First，就它啊太，它这个校名是叫 First，我们这个已经建了是吧？啊，建了。Sorry 啊，刚才有些事情啊，没跟上。我们讲到这个 SRC 了是吧？建立一个那个资源啊资源包。在资源包里面，我们把它单机打开，建立一个类啊，这个 Scala Scala class。Scala class。我们来测试一下啊，简单的先测试一下。那比如说这里的太 And then finish 创建这个类之后啊，我们也看到它跟我们 Java 里面所生成的那个 class 类是基本上是一致的啊。基本上是一致的。它也是生成，不过它，大家可以看到，不过它的这个后缀名啊，是 Scala 啊，是 Scala 它有自己的一个特点。
说话人 1
第二大类呢，到我们在中后期，中期的时候，给大家主动地来讲一下。主动地来讲一下，因为这个 Scala 类啊，它在讲类之前呢，我们要先讲这个对象。在 Scala 这个语言当中呢，对象比我们这个 Java 当中讲的对象要难很多。我们首先来看一下我们上节课讲这节课刚讲这个变量。比方说我们定一个变量 I 它等于一个10，10。然后再定一个 VR 接它等于一个11，假设。这边我也看到啊，我们在定义这个变量 i 和变量 j 的时候，后面我们省略了一个分号啊。省略了一个分号。在 Scala 编程语言当中啊，它有一个在书在这个作者当中啊，这个写书的时候，在开发这个语言的时候啊。它有这么一句话说。尽量减少这个程序员的这个手动操作，尽量尽可能地减少，因为他认为多打一个空格啊或者多打一个符号啊都是非常复杂的事情。所以说他能减少就减少。所以说这对于我们在编写 Scala 的这个应用程序的过程当中呢，是非常重要的一个事情。
说话人 1
它的这个两个变量啊，一个是 i 一个是 j 对吧？v a r 定义是我们的变量，v a l 定义是长怎么样？那么我们现在来定义一个常量，VAL，比方说我们是 K，K 它输出的是什么呢？比方说是 I 加上 J 啊，J 啊是这样的。那么我来看呢，现在啊，我们就有了这么三个啊，三个三个三个三个量，三个变量啊，两个变量。一个常量啊。后面呢没有这个，没有这个分号啊。那我们在这个 Java 里面的时候呢，我们是用这个 main main，也就是我们的入口函数入口函数来输出。在我们的整个的这个 Scala 里面呢，我们有一个用它的这个测试对象，也就说伴生对象来说啊。
说话人 1
首先呢到讲对象的时候，给大家讲一下什么是半生对象，什么是对象。让大家先简单的知道一个 objects 啊，比方说我们做它的一个这个，如果是名称一样的时候呢。就是它的半生对象。在这里面我们来做一个啊，这个 class，加上这个。在这个半生对象里面呢，我们来做这个密密方法。在我们这个 Java 里面，入口函数是 public static void mainString args。而在我们这个 Scala 里面，直接用 def D、E、F 是定义函数的关键字，它是 mean。然后呢跟我们 Scala 里面，跟我们 Java 里面是一样的，Args，对吧？然后呢它是一个 array。 
说话人 1
当我们定义完这个入口函数的时候呢，比方说我们现在定义一个对象，岗位，加了一个 C。C，它等于什么呢？new test。 New class 这个地方呢，大家注意一下啊，这个一定要背起来。我们在学这个 Java 的时候，我们是 void main，对吧？String args。但这个不一样了啊，这个我们看，这个 def main 后边，Args 是在前面，我们说这个 Args 是 argument 的缩写。明白吧？是参数。然后呢后面是什么？Array Array 是什么？是我们的列表哈。
说话人 1
然后呢列表里面的属性是什么东西啊？是字符串类型的，清楚了吧？哎，就是说什么意思啊？这个 Args 就是说我们这个参，这个 Args 是参数，参数的名称。参数的名称它的类型是什么类型啊？是 Array 类型的，是列表类型的。那列表里面的这个元素是什么类类型啊？是String 类型啊，懂了吧？与我们的 Java 是不一样的啊，你这个写错了，写成 Java 类型的就错了。记住了吧？哎，它是什么？D、E、F、main，然后，A、S，冒号，然后，A、R、E、N，然后这个基本的这个叫什么？Python。 好，在这里，这个它是我们在这个学习 Java 的时候啊，new 它的什么是构造方法。
说话人 1
构造方法对象。那也就是我这个 c 呀，在这里是一个对象啊，是一个对象，而它在这个前面，我把鼠标放在这上面。这个地方的时候我们可以看到它是什么？它是 V A R C 冒号开始，对吧？V A R C 冒号开始，这是非常重重要的事情。V A R 是它的关键字，C 呢是这个变量的名称，冒号后面是它的类型，那什么类型呢？ Test，那 test 什么呢？是 class，也就是它是个类的对象。
说话人 1
那我们再来说一下啊。我们说呀，在 Java 的时候呢，new 是什么？创建内存空间，也就是说我们的构造构造方法，对不对啊？ new，比方说类名，是不是构造方法呀？Constructor，构造方法。而我们说在构造方法前面，我们说前面这个东西是 class name，然后 out 是 name。对不对？是类名、对象名、等号、new、或者方法，是不是这样啊？而在我们这个 Scala 里边，你看它说任何一个都称之为什么对象。就是我们这个 VR 是声明一个变，声明一个变量的，对吧？那么任何一个变量呢，它都可以是任何的什么类型。它的类型是由谁来决定的呀？是不是由后面赋值的东西来决定的？而后面赋值的东西是什么？new test 是什么类型啊？是不可，是 test 的类型啊？是类的类型，是 test 的子类型啊。我们在学 Java 的时候，我们说这个你定义那个 class，是不是特殊的类型啊？是吧？你比方说我们说 class student，那这个 student 的 p，那这个 p 或者 student 的 s。这个 s 就是对象。这对象什么类型？是不是 student 的类型啊？对吧？跟我们的 int i i 什么类型， int 类型是一样的。而这个 student 类呀，是你定义的一个 type face 也就是说你定义你的类型。而这个变量就是你的对象，对吧？我们说 Java 是这么讲的啊，这个也是一样的。
说话人 1
我这个 c1，我们说variable 是什么？是声明一个变量，而这个 c1 啊，就是变量的名称。而它的 type 是由谁来决定的？是 value type 决定的，是后面决定的。所以说我们在 Scala 里我们我们注意的是谁啊？是这个变量本身，而不是它的类型。类型是 value 给的是什么东西，它就是什么东西。也就是说我这个 c1 我可以是 new test。 我可以是 int，我可以是 double，我可以是任何的类型，对不对啊？因为前面我只声明了一个，word，是不是变量名啊？没有类型，那类型是，那它复制一个 new test，那它的类型就是 new test。那 test 的什么类型呢？就是 class 的类型。类的类型。
说话人 1
所以说既然是类的类型呢，那我们 C1就是什么？就是我们的变，就是我们的对象，就是 object 对不对啊？哎，就反过来套到我们的 Java 的这个思想里面。所以说，C1是 Java 的一个升级版啊。就是说，升级就复杂简化版了，某些地方思想变得复杂了一些，但是代码写的确实不能说简化。优化，对吧？
说话人 3
哦。
说话人 1
有了这个对象之后啊，我们可以做什么事情呢？比方说我们要输出输出这个 key，值是N加N。然后呢我们来做一下啊，试一试。
说话人 2
只有 k 啊，保持保持一下啊，是 s 啊。 
说话人 1
s 做口到这个地方我说一下啊，之前没有讲啊。记住了，我们在 Java 的时候，我们说我们声明的类啊，如果它的这个修饰符没有写出。标明的。修饰符有三个，public、private、protected，对吧？如果没有标明的时候，那它的这个修饰符是不是缺省？缺省，对吧？缺省的。而我们这里边你记住了，我们所有的类，缺少的都是什么？都是 public 类。清楚了吧？都是公有的，也就是说我的类的可见性是公有的，记住了吗？可见性与我们的 Java 的可见性是一样的。
说话人 1
再比如，这个 Scala 我就不说了，就是讲到那的时候也不讲了。如果你不会的话，你把笔记打开，那个 Java 的笔记是一样的，就是 public、 protected 和 private。它们的可见性是一样的。跟我们它是一样的，不同的地方就是我们大数据的，不是大数据的，我们这样就是我们这个 Scala 它是共有可见性。为什么共有可见性？这个东西是与我们大数据是有关联的。就是说，他认为处处皆可见，处处被调用。你比方说，我们有这么多个同学，每一个人的这个权限都不一样的话。是不是就要设置权限呢？而对于大数据来说，它是非常开放的，它的权限是平等的，谁都可以用。所以一般情况下，它的这个这个权限都是 public。 那你用什么来定义它的权限呢？我后面会说啊。所以说它是很什么啊？很广泛的一个东西，就谁都可以看见。既然可以看见，它就可以调用。既然可以调用，我们就可以是给它赋值啊、取值啊，是不是方便多了？
说话人 2
好。
说话人 1
从，保存之后啊，我们来做一下啊。
说话人 2
运行。
说话人 1
啊，你看他写的是2，他就出来了。对吧？对吧？好，那我们现在每一台笔记本上都有了，他打开建立一个建立一个项目，这个项目是什么呢？你比方说我一开始赋值，创建一什么？创建一个列表。赋值是110吧。可以吧？啊，110。然后我们输出输出偶数项。清楚了吗？就是我们输入的时候是一、二、三、四、五、六、七、八、九、十，对不对啊？我要输出偶数项，就是第二项、第四项。第6项、第8项、第10项，听懂了吧？
说话人 2
好，做这个题。
说话人 1
如果不会，哪个地方不懂，就问一下小豆包啊。
说话人 3
小豆包。嗯。我操，这个东西是你自己贴的吗？对，自己花在网上买的，贴的，花了十几块买的。我操，那这是一个一个贴的吗？还是一整个往上一贴？这键盘一个一个个贴的，我操，贴了两个多小时呢。
说话人 2
我也带了，我没下我 QQ。
说话人 1
你坐那看不见，你不能看见吗？把笔记本笔记本往中间拽哈，你们两个往前看，不行就站起来啊。
说话人 1
哎，你们这排一个也没拿呀。你是人那排的，你往前伸什么手啊？你们3个是原来坐哪的？
说话人 2
嗯。
说话人 1
那个同学，你坐这，你坐这，你坐后面一排，你坐过来，你拿着笔记本上这来，然后你们这排就有了。你们俩换一下。
说话人 3
被调走了，我要走了。
说话人 2
好的好的好的好的好的。
说话人 4
你拿手机摄像头放大看。
说话人 2
你有没有讨论起来坐一坐啊，讨论起来坐一坐。
说话人 2
哦。 
说话人 2
Hello baby 偶数的英文。
说话人 2
哦，好的。
说话人 2
喜欢脸红，你帮我背吗？
说话人 2
10个数，输入10个数，输出哦。
说话人 1
我也都上不去了。
说话人 2
哎呀，别玩了。
说话人 1
哎，不要说话啊，现在大多数犯了同一个错误。注意听啊，有很多人呐，在这个地方上，你看你写了一个类是吧？哎，然后你就就运行，然后的确它是没有错误的，但是你想进这个，你想我们在讲 Java 是不是有一个入口啊？就是这个 main 呢，你必须得有 main 方法，你才能把这个项目运行起来。你现在你这个项目里边没有 main，所以它弹出了一个框，它说我找不到入口，自己得定义一个。就是你看它在下面呢，option 下面，D、E、F main 嘛。我刚才讲的就是这个嘛。我说它有一个 axis，这个 axis 它的类型是是 a ring，然后这个是列表。然后这个里边的元素是什么啊？ String 是不是啊？哎，你得把这个抄上去，然后去做这个事情。
说话人 1
这不是入口吗？就是你要写一个类，然后呢你再写一个对象，你看我这个格式看见了吧？看没看见我这个格式啊？或者你不写我这个类，直接用 object 做也可以。完全没问题，明不明白呀？关键是要有入口，懂了吧？懂不懂？哎，坐一下吧，再坐一下。一看就是你人工给整的。
说话人 1
对，你把这个全屏一下，让他们看一看。嗯，嗯。
说话人 5
干啥呀？突然整这死出。能不能全屏？没事你学吧，我买单。
说话人 3
你们可以看一看。
说话人 1
我讲课没用，你得先按我讲课来。
说话人 1
他们不会给他们讲一讲啊。哦。
说话人 3
其实其实我我我。
说话人 5
你是不是也不是很会？
说话人 3
对，我也我也不是很会。
说话人 5
复制粘贴发给周东耀，谢谢。
说话人 1
有同学做的不错啊。
说话人 3
有同学做的不错啊。中药。
说话人 5
发给苏志文也可以。
说话人 1
你看不见人家一直走，我也想看一看。
说话人 3
啊？我不知道我怎么对对不对。
说话人 3
发过去了发过去了。
说话人 1
没有啊。来，你你们三个谁拍一下，给他整一下。给他们整一下整一下，你回去问一下，他们整出来了。
说话人 2
整没整出来？
说话人 1
快差不多了，快了啊。
说话人 4
哎，慢一点走。
说话人 1
累是累，在 Java 里面累是累，对象是对象。
说话人 4
现在走出来的同学听着。
说话人 1
坐出来听着，把奇数项相加求和输出啊。继续，奇数项相加求和输出。别歇着写啊，再给我创建一个，就熟悉这个过程。再创建一个啊。听清楚我的问题了吧？不要接着做啊，再创建一个新的项目。
说话人 2
你瞅瞅。
说话人 1
继续讲，再创建一个啊。
说话人 2
再创建一个。
说话人 2
没有能用11吗？
说话人 1
能用123吗？
说话人 2
能用456吗？
说话人 2
咱们讲课的时候，要求是尽量有没有做出来？
说话人 1
你做没做出来？没有啊。奇数项的和有没有做出来的？这奇数项和说出来，做偶数项和奇数项相乘的和，是第一项乘第2项，第3项乘第4项，第5项乘第6项。明白吧？第七项乘第八项，第九项乘第十项，乘积，然后相加。明白了吧？然后再创建一个。
说话人 6
你英语四级过了吗？
说话人 5
过了，啥时候过的？
说话人 3
这个是刚过。
说话人 5
我不是对不起你啊，我走了。讨厌你讨厌你一个人自己过的人。
说话人 3
你下次努力就行，马上就能报了。
说话人 2
啊，我到机场了。
说话人 4
你不用那个。
说话人 4
我给你就说二十五。
说话人 4
我觉得你不是你也在你的吗？我老公在你的吗？但是我们的产品和你也不一样。我以为要跟你俩啥的？ 
说话人 4
I love you 你说一下，你说大声一点。
以上文本由 AI 总结生成

