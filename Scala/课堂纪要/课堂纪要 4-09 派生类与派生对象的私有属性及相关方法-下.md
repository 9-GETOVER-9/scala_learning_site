04.29 15:24
4 09
派生类与派生对象的私有属性及相关方法
2026-04-09  16:06
【智能总结】
本次课堂主要讲解派生类和派生对象的私有属性及相关方法，布置总结方法的学习任务。
- **私有属性的使用范围**：私有属性只能在定义它的类结构中使用，但派生类和派生对象也能访问，类体以外不可见。
- **派生对象与静态方法**：派生对象类似静态方法和属性，用类名调用，属于类层级别的内容。
- **组合器相关**：在类名后与类交织的是组合器，加特定标识可将参数升级为属性。
- **apply方法测试**：使用apply方法创建对象，按参数数量对应传递，能产生类对象并调用类方法。
- **学习任务**：要求学生通过网络平台总结相关方法，10点半检查。
【原文】
说话人 1
我们来看一下啊，派生对象和派生类的私有属性，派生类和派生对象私有属性。他怎么说的呢？他是这样的。比方说呀，他这是一个派生类，派生类，那它主要是说私有属性，我们就看私有区分就行了。它私有属性是一个 color 对吧？这个 color 呢是 blue 就是这么一个东西，对吧？好，那我这边呢，它也有一个，比方说它这个是 com。是吧？哎，私有属性。好，进入了另一种方法。
说话人 1
那第一个，这这句话，这是整了个什么东西啊？ New class and open new 是什么东西啊？那类对象是不是啊？你看，类对象，也就是我们创建对象嘛。非常简单，有个 C 呀，就是我们的对象，啊，C 对象。那 C 呢？我们看 C 点 id，由于 id 不一样，我们要做一个操作。调用半程位当中的非成员函数，调用成员函数，在这呢。他你看在这，半人类的什么？私有属性，C1点错了，是不是啊？我们说我们说我们是在这个，是这样说的。我们说呀，私有属性，啊。私有属性只能在定义它的立体结构当中去使用，对不对？哎，那为什么在这个我们半生对象，半生对象也可以使用呢？就是因为它们是一体的。一起的就是为什么没给大家说了是吧？所以说这个地方啊是可以调用的，否则的话，如果这个地方是其他的名字。他就错过，对不对啊？也就是说我们 C 呀，在非，就是不是我们定义私有属性这个类的类外，叫类体以外的。它的可变性是不可见的，对不对？哎，是不可见的。所以只有在我们的半生类和半生对象当中，我们这个才能访问它的私有属性和私有方法。
说话人 1
当然， C 的 color 调用它的方法也是可以的，对吧？哎，可以的。就是说它们是可以进行调用的。包括这个在这呢啊，这个少写了一个 N 啊，少写了这个 N 呢，看到了吧？少写这个 N 这个 class and object 它是什么东西啊？是半径最佳吗？清楚了吧？哎，办证对象调用的私有方法。哎，千万不要以为这个东西是类的名称啊，不对啊。是半生对象。这，换一句话来说，我们说半生对象里面的东西，就是我们以前所说的 static 的静态方法。
说话人 1
静态属性。那静态方法、静态属性以前是用类名来进行调用的，对不对？我们叫类属性。所以呢，我们用半生对象，是，还是你会看到，是，是不是跟我们的类名是一样的呀？那也就说它还是我们的类层级别的一些东西，只不过用半生对象来表示。明白了吗？哎，其实。跟我们 Java 里面的思想是一样的，只不过它更加细化，更加细化。它将我们的非非非静态。和静态，它俩拆分开了。有什么好处呢？还是为了主不漏气。
说话人 1
由于我们这个类呀，看， class and object 是不是主不漏气呀？由于它是主构造器呀，所以它将静态的东西拿出去了。因为我们在构造对象的时候，就是主构造器构造对象的时候。如果说我们对象能构造出来，我们是不是用对象来访问我们的这个这个方法呀？但是呢，我们的这些某些了，我这么说，有某些属性和方法，我更希望用类名来进行调用。所以我将它放到了外层对象里面。这个时候，当我们在调用我们的主路由器构造对象的时候。他是不是就不访问我们的静态方法了？所以说我们直接用我们的半程对象，来调用我们的，就所谓的静态方法。
说话人 2
其实语言特点，一个是适用于中文的模型架构，还有标点符号。
说话人 1
嗯，为什么去做这个事情？
说话人 1
编程人为必须让本类对象复写一个这个方法，因为这个这个方法呀，它的参数对应在对应本类的构造器的参数列表上。我们来看一下，什么意思呢？在类名后面，与类交织在一起的，就是组合器。也就是说，这一堆呀，是不是组合器呀？在类名后面，与类交织在一起的组合器。如果没有加这个 word 呢，就是只是过了参数。如果加上 word 呢，就升级为属性。我们简单来看一下吧。那首先呢，第一个啊，这就是我们的一个就是说构造一个参数，参数列表，那么它将它打印出来。负责 work，来负责有一个主热器是吧？主热器这不是两个吗？啊，是不是第一行应该是主热器啊？哎，两个，A，name，放进来。
说话人 2
这样能更好地理解和执行命令。
说话人 1
然后我们来看一下啊，他怎么说的？他说我们来测试一下， apply 方法，啊，使用 apply 方法创建。哎，这是我们的什么啊？然后呢，哎，这是什么？ a play test，是它的什么？一个类型，返回的类型啊，对不对啊？写的对方法啊，所以我们就一定要看看这个，是什么东西，是在重写的啊。你看啊，他将我们两个参数，一个是 name，一个是 age，传进来。然后呢，它返回一个，play test，是我们那个类对象，对吧？在这呢，是不是类对象啊？那这个里面是不是两个参数啊？然后它将这个两个参数传进来。也就是说，它这个当你调用这个外形对象的时候，那它直接就调用了什么？产生了类对象，而且它将这两个什么东西啊？参数传递进来。对吧？而且它参数是从这来。
说话人 1
第二个，这三个参数跟它是一样的，哎调用了辅助路由器，哎，这这三个对不对啊？然后你会看到，他这两个地方啊，是不是没有 new 啊？就没有 new 啊。是主动的记忆，第二个叫也就是说，他调用test test 这个 PlayTest 对象当中的 Play 方法。就是我们啊这个 OK 方法啊，就这个。
说话人 1
其实你可以这么理解啊，就是说我们如果有三个对象呢，不是，三个对象，三个参数的，那它就对应三个参数的。如果有两个参数的，就对应两个参数。这个是我们的这个分类方法。对于我们半生类和半生对象，其实我们所关注的呢就是这一个，所以说我们在变成对象当函数的一个功能去构造啊。
说话人 1
过了我们的这个对象，这个不对方法是很重要的。它呢，你们看一下，它是返回什么？返回我们的主过滤器呀。就是说，当我们做这个事情的时候，非方法的时候，它是返回我们的 new Play test。这个 new Play test 不是主路由器。对不对啊？哎，这是我们 Java 里的那个什么步骤方法。
说话人 1
所以说我们当我们定，调用这个，可力可力的时候，它是产生了一个类对象。产生了一个对象。所以说我们可以用我们的半生对象来调用我们的对象，产生我们的类，不对呀，用我们的半生对象调用我们的方法产生类对象。然后呢用我们的类对象，比方说产生类对象之后呢，我们赋值给一个变量，这个变量就是类对象。我们再用类对象呢，来调用我们的类方法，就是类里边的方法。清楚了吧？哎，其实是这样的。所以就是说我们在这一章啊，还是重点就是说我们如何重写这些方法。就是说如果这个地方啊如何产生半人对象？哎，我如何写？因为他这地方有个主路由器在里面。就是我如何构建，就是在我们的方法里面，如何写我们的主路由器。
说话人 1
一年级的题我不会。还有点时间啊，还有点时间呢，你们现在就做这个事情啊，就做这个事情，就是说将我们的这方法。你们给他用，先用，无论是用豆包也好，用千问也好，还是用我们那个百度也好。还是什么东西，你们给他总结到你们的笔记本上。然后我们就是，咱们几点下课呀？50是吧？咱们10点半我检查啊，我检查一下。就每个人都要做这个事情，好吧？先去听，先去听啊。因为我讲的呢，没有网上讲的好。比如说你们要讲，要去签问一下豆包。啊，或者百度，菜鸟啊，或者是 B 站呐，多去这些地方，多去听一听。听完之后，那你们集众多之所长，把这个方法总结出一个文档。写到你的笔记本上。那你看看他到底在做什么事情。清楚了吧？哎，这个地方，你把这方法整明白了。
说话人 1
其实我们半生类半生对象也就没有什么难的，它难就难在这个，你在写的时候，你知道这个方法位置。就是我们写的像，我们写半生对象半生类，我们的属性和方法跟我们 class 是一样的。唯一不同的就是目标和内容。怎么写呢？很重要。先去看，先别着急写啊，先去看。我看看，我还不知道呢。
以上文本由 AI 总结生成

